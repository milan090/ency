name: ency

services:
- name: nest-api
  github:
    repo: milan090/ency
    branch: develop
    deploy_on_push: true
  source_dir: ency-backend
  dockerfile_path: ency-backend/Dockerfile
  run_command: yarn start:prod
  routes:
  - path: /api